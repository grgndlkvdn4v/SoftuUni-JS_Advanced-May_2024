import { expect } from "chai";
import { sum } from "../exercises/4. Sum of Numbers.js";

// describe("Test with numbers arr", () => {

//   it("sum positive numbers", () => {
//     expect( sum([1, 2, 3, 4, 5]) ).to.equal(15);
//   });

//   it("sum negative numbers", () => {
//     expect( sum([-1, -2, -3, -4, -5]) ).to.equal(-15);
//   });

//   it("sum empty array", () => {
//     expect( sum([]) ).to.equal(0);
//   });

  
//   it("sum zeros arr", () => {
//     expect( sum([0, 0, 0, 0, 0]) ).to.equal(0);
//   });
// });
